import { HoverTrailDirective } from './hover-trail.directive';

describe('HoverTrailDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverTrailDirective();
    expect(directive).toBeTruthy();
  });
});
