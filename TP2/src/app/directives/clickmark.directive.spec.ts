import { ClickmarkDirective } from './clickmark.directive';

describe('ClickmarkDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickmarkDirective();
    expect(directive).toBeTruthy();
  });
});
